# M3-A4B Mac Bootstrap Runbook

## M3-A4B2A validated flow

1. Require the exact injected pytest root below `/private/tmp`.
2. Reject pre-existing application-state and restore-validation targets.
3. Validate the complete M3-A4B1 synthetic permit and exact bindings.
4. Atomically claim the synthetic permit through the injected test registry.
5. Create and permission the test-only application-state layout.
6. Bootstrap and read-only validate audit and replay databases.
7. Create baseline backups/manifests and validate separate restores.
8. Derive non-persisted PRE_ACTIVATION monitoring evidence.
9. Verify all activation counters remain zero and return canonical evidence.

This procedure does not authorize macOS Application Support execution. Do not
substitute an operational root, issue an operational permit, activate writers
or monitoring, dispatch alerts, invoke Ubuntu, or run infrastructure commands.
M3-A4B2B is the separate next authorization and execution task.
# M3-A4B2B2B-R1 recovery compatibility

The application-state parent is shared and may preexist under the strict
shared-parent policy. Deployment bootstrap owns only absent `audit`,
`security`, and `monitoring` children. Existing siblings and parent metadata
must remain unchanged. The recovery inspection result is
`ROOT_EXISTS_SAFE_PARENT_CANDIDATE`; operational bootstrap remains not
executed and requires fresh approval and permit.

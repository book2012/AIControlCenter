# M3-A4B2B2B-R2 Controlled Operational Activation Boundary

Status: capability validated; operational activation not performed.

The earlier M3-A4B2B2B attempt ended `BLOCKED_PRE_PERMIT`. Its
`LIVE_ISSUANCE_PROHIBITED` and
`CONTROLLED_MODE_NOT_INVOKED_BY_VALIDATION_RUNNER` results were
architecture-correct. R2 does not remove either default-deny property. It
introduces a separate immutable authorization artifact binding exact Git,
human approval, permit-review, independent identities, restrictions,
shared-parent policy, absent target, runtime plan, schemas, tests, safety,
time window, controlled scope and one use.

Non-synthetic issuance requires a valid non-synthetic activation
authorization. Controlled execution requires a matching authorization,
permit, issuance evidence, trusted path, branch, commit, timestamp and the
approved Mac runtime adapter. Test execution remains confined to injected
pytest roots and the test adapter.

No real permit was issued or claimed. The actual Mac bootstrap was not
executed; managed targets remain absent. Writers, monitoring and dispatch
remain inactive. Production activation is `NOT_AUTHORIZED`. Because the
authorization binds an exact commit, fresh independent approval is required
after this increment's commit.

Next: `M3-A4B2B2B Fresh Approval and Authorized Mac Bootstrap`.
